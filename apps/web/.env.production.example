# Copy to .env.production.local for production Worker builds (do not commit secrets).
# These VITE_* values are baked in at build time.

VITE_GRAPHQL_URL=https://api.example.com/graphql
VITE_AUTH_URL=https://api.example.com/auth

# Also set on the Worker at runtime (wrangler secret put DB_AUTH_SECRET):
# DB_AUTH_SECRET=
